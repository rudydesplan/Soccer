# Backend API dependencies — pinned to the versions in ../../requirements-ml.txt
# (the backend imports salary_benchmark, which needs AutoGluon for inference).
fastapi==0.139.0
uvicorn[standard]==0.49.0
pandas==2.2.3
numpy==2.3.5
pydantic==2.13.4
scipy==1.16.3
autogluon.tabular==1.5.0
# `pip install autogluon.tabular` alone does NOT pull in lightgbm — it's an
# optional extra, not a hard dependency. Every served model (full + all 3
# fallbacks) is a LightGBM/WeightedEnsemble — confirmed via
# trainer.get_minimum_model_set(model_best) — so this is required for any
# prediction to succeed, not just a nice-to-have.
lightgbm==4.6.0
# Per-prediction explanations (POST /api/benchmark/explain).
shap==0.52.0
